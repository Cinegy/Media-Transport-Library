#pragma once

#define MTL_COMPILER "${MTL_COMPILER}"

#define MTL_VERSION_EXTRA "${MTL_VERSION_EXTRA}"

#define MTL_VERSION_LAST ${MTL_VERSION_LAST}

#define MTL_VERSION_MAJOR ${MTL_VERSION_MAJOR}

#define MTL_VERSION_MINOR ${MTL_VERSION_MINOR}
