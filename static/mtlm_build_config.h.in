#pragma once

#define MTLM_COMPILER "${MTL_COMPILER}"

#define MTLM_VERSION_EXTRA "${MTL_VERSION_EXTRA}"

#define MTLM_VERSION_LAST ${MTL_VERSION_LAST}

#define MTLM_VERSION_MAJOR ${MTL_VERSION_MAJOR}

#define MTLM_VERSION_MINOR ${MTL_VERSION_MINOR}
